<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.dv.kotlin.kotlinexcersice">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
            android:allowBackup="true"
            android:appComponentFactory="ComponentFactory"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:replace="android:appComponentFactory">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activities.LifeCycleActivity"/>
        <activity android:name=".activities.ClickEventActivity"/>
        <activity android:name=".activities.AndroidExtensionActivity"/>
        <activity android:name=".activities.PicassoActivity"/>
        <activity android:name=".activities.ListViewActivity"/>
        <activity android:name=".activities.IntentsActivity"/>
        <activity android:name=".activities.PermissionActivity"/>
        <activity android:name=".activities.SharedPreferenceActivity"/>
        <activity android:name=".activities.ExtensionFunctionActivity"/>
        <activity android:name=".others.LifeCycleEventsActivity"/>
        <activity android:name=".activities.IntentExtrasActivity">
                <meta-data android:name="android.support.PARENT_ACTIVITY"
                    android:value="com.dv.kotlin.kotlinexcersice.MainActivity"/>
        </activity>
    </application>

</manifest>